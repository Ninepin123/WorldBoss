# 世界 Boss 插件需求文件（World Boss System Spec）

## 一、專案目標

建立一套可與 MythicMobs 整合的世界 Boss 系統，包含 Boss 定時生成、傷害排行榜（使用 DecentHolograms 顯示）、戰利品分配與完整循環機制。

---

## 二、核心系統功能

### 1. Boss 生成系統

**功能描述：**

* Boss 綁定 MythicMobs 怪物
* 於「指定座標」生成

**生成方式（需同時支援）：**

1. 固定間隔生成

   * 例：每 X 小時生成一次（透過 config 設定）
2. 固定時間生成

   * 例：星期四 00:00

**生成邏輯：**

* Boss 死亡後 → 才開始下一輪倒數

---

### 2. Boss 存在與消失機制

**規則：**

* Boss 生成後「3 分鐘內無玩家攻擊」
  → 自動消失（Despawn）
* 消失後：
  → 直接進入下一輪生成倒數

---

### 3. 傷害排行榜系統（DecentHolograms）

**顯示插件：使用 DecentHolograms**

每隻 Boss 必須有「獨立排行榜」，不可共用。

#### (1) 即時傷害排行榜

* 使用 DecentHolograms 顯示浮空文字
* Boss 戰期間即時更新（建議每1~3秒刷新）
* 顯示內容：

  * 玩家名稱
  * 傷害數值
  * 排名（Top X）

#### (2) 歷史傷害排行榜

* 使用 DecentHolograms 顯示
* 紀錄該 Boss 歷史最高傷害
* 長期保存（需寫入檔案或資料庫）

#### 顯示規則：

* 即時排行榜 與 歷史排行榜「分開顯示」
* 每隻 Boss 各自擁有兩個全息投影（Hologram）
* 可設定顯示位置（座標）

#### 額外需求：

* Boss 死亡後：

  * 即時排行榜可選擇隱藏或清空
* 排行榜需支援：

  * Top N 顯示（可設定）
  * 名稱顏色 / 標題格式

---

### 4. 戰利品箱系統（Boss 擊殺後）

**觸發條件：**

* Boss 被擊殺

**功能描述：**

* 於指定位置生成一個戰利品箱

**開啟條件：**

* 僅限「有對該 Boss 造成傷害的玩家」可開啟

**物品分配機制：**

* 所有玩家共用同一個箱子
* 採「先搶先贏」機制（非個人掉落）

---

### 5. 掉落設定系統

**設定方式：**

* 指令設定 Boss 掉落
* GUI 介面編輯掉落內容（可拖拉物品）

**功能需求：**

* 每隻 Boss 可獨立設定掉落池
* 每個物品支援：

  * 掉落機率（%）

---

### 6. GUI 管理系統

* 可透過 GUI 編輯掉落物
* 可設定掉落機率
* 減少指令操作

---

## 三、系統流程（完整循環）

1. 倒數開始
2. 時間到 → Boss 生成
3. 顯示「即時傷害排行榜（Hologram）」
4. 玩家攻擊 → 即時更新傷害
5. Boss 被擊殺 或 無人攻擊消失

**若擊殺：**

* 更新歷史排行榜（Hologram）
* 生成戰利品箱
* 玩家搶奪戰利品

**若無人攻擊：**

* Boss 消失
* 不觸發掉落

6. 進入下一輪倒數

---

## 四、可設定項目（Config）

* Boss 生成座標
* 生成方式（時間 / 間隔）
* 無人攻擊消失時間（預設 180 秒）
* 掉落物與機率
* 排行榜顯示數量（Top N）
* Hologram 顯示位置
* Hologram 樣式（標題 / 顏色）
* 是否顯示 / 隱藏即時排行榜

---

## 五、技術需求

* 相容 MythicMobs
* 整合 DecentHolograms API
* 排行榜資料需可持久化（檔案或資料庫）
* 支援多 Boss 獨立設定
* 指令 + GUI 操作

---

## 六、最終交付內容

* 插件主程式
* config 設定檔
* Hologram 顯示設定
* GUI 管理介面
* 指令說明文件

---

## 七、備註

* 每隻 Boss 需有獨立：

  * 即時排行榜 Hologram
  * 歷史排行榜 Hologram
* 排行榜需避免重啟遺失
* 掉落設計需避免資源通膨（已採搶奪制）

---

**版本：v1.1（已整合 DecentHolograms）
用途：開發確認 / 委託規格書**
